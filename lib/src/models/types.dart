/// {@template segmented_bar_type}
/// The type of the progress bar that needs to be displayed
/// {@endtemplate}
enum SegmentedProgressBarType {
  /// A standard linear progress bar
  linear,

  /// A standard circular progress bar
  circular;
}
